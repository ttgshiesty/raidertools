export const normalizeItem = (it) => it;
export const normalizeEnemy = (en) => en;
export const normalizeMap = (m) => m;
export const normalizeUCIID = (id) => id;
export const normalizeProjectProgressSnapshot = (s) => s;
export const normalizeProgressSnapshot = (s) => s;
export const deriveExpeditionStatus = (s) => s;
